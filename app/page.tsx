export const metadata = { title: "Home  App Router" };

export default function Home() {
  return (
    <main>
      <h1>/ HOME — App Router</h1>
      <p>If you see this, the root route is wired correctly.</p>
    </main>
  );
}
