cd "C:\Users\doitr\OneDrive\Desktop\radiant-ts"
npm pkg get name   # should print "radiant-ts"
npm run dev        # should start on http://127.0.0.1:3000
npm run backup     # should create ..\radiant-ts-backup.zip



cd "C:\Users\doitr\OneDrive\Desktop\radiant-ts"

# Kill anything on 3000
$port=3000
Get-NetTCPConnection -State Listen -LocalPort $port -ErrorAction SilentlyContinue |
  Select-Object -ExpandProperty OwningProcess -Unique |
  ForEach-Object { try { Stop-Process -Id $_ -Force } catch {} }

# Clear Next cache
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue

# Start dev pinned to IPv4:3000 (uses the dev:local script)
npm run dev

CANNOT REACH.
npm run dev:3001
